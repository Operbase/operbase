'use client'

/**
 * AnimateIn — Re-export from shared animations for backwards compatibility
 * @deprecated Use AnimatedSection from '@/components/shared/animations' instead
 */

export { AnimatedSection as AnimateIn } from '@/components/shared/animations'
